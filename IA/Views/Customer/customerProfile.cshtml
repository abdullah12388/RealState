@model IA.ViewModel.Customer_Assingnd_NotAssigned_Projects

@{
    ViewBag.Title = "customerProfile";
    ViewBag.user = Model.CustomerData;
    Layout = "~/Views/Shared/_CustomerProfile.cshtml";
    var count = 0;
    var count1 = 0;
}

<div class="col-md-7 order-2 order-md-1">

    @foreach (var project in Model.Assigned)
    {

        if (count == 0)
        {
            <h2>Assigned Projects</h2>
            count++;
        }
        <div class="property horizontal d-flex">
            <div class="prop-details p-3">
                <div><a href="#"><strong class="price">@project.pName</strong></a></div>
                <div class="">
                    <h3>Salary :<strong style="color: black">@project.pSalary</strong></h3>
                    <h3>Discription :<strong style="color: black">@project.pDescription</strong></h3>
                    <h3>Area :<strong style="color: black">@project.pArea</strong></h3>
                </div>
                <div>
                    <a href="#" class="btn btn-primary btn-sm">Assigen To Project Manger</a>

                </div>
            </div>
        </div>


    }

    @foreach (var project in Model.NotAssigned)
    {

        if (count1 == 0)
        {
            <h2>Not Assigned Projects</h2>
            count1++;
        }
        <div class="property horizontal d-flex">
            <div class="prop-details p-3">
                <div><a href="#"><strong class="price">@project.pName</strong></a></div>
                <div class="">
                    <h3>Salary :<strong style="color: black">@project.pSalary</strong></h3>
                    <h3>Discription :<strong style="color: black">@project.pDescription</strong></h3>
                    <h3>Area :<strong style="color: black">@project.pArea</strong></h3>
                </div>
                <div>
                    @Html.ActionLink("Delete", "DeleteProject", new { id = project.Id }, new { @class = "btn btn-danger" })
                    @Html.ActionLink("Update", "Edit", new { id = project.Id }, new { @class = "btn btn-warning" })
                    <a href="#" class="btn btn-primary btn-sm">Assigen To Project Manger</a>

                </div>
            </div>
        </div>


    }
</div>

